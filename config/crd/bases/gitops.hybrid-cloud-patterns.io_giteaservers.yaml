---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.14.0
  name: giteaservers.gitops.hybrid-cloud-patterns.io
spec:
  group: gitops.hybrid-cloud-patterns.io
  names:
    kind: GiteaServer
    listKind: GiteaServerList
    plural: giteaservers
    singular: giteaserver
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: GiteaServer is the Schema for the giteaservers API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: GiteaServerSpec defines the desired state of GiteaServer
            properties:
              chartName:
                description: 'Chart Name that we will deploy. Default: gitea-chart'
                type: string
              helmChartUrl:
                description: 'Helm Chart URL. Default value: https://charts.validatedpatterns.io'
                type: string
              releaseName:
                description: 'Release name used to deploy the chart.  Default: gitea'
                type: string
              repoName:
                description: 'Helm Repo name. Default: helm-charts'
                type: string
              version:
                description: 'Version for the chart. Default: 0.0.3'
                type: string
            type: object
          status:
            description: GiteaServerStatus defines the observed state of GiteaServer
            properties:
              chartStatus:
                description: Status of the chart
                type: string
              lastError:
                description: Last error encountered by the pattern
                type: string
              lastStep:
                description: Last action related to the Gitea deployment
                type: string
              route:
                description: Route for the service
                type: string
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
